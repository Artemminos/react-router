import React from 'react';

const compareFn = (prev,next) => {

}
const Main = React.memo(() => {
    const a = ()=>{

    }

    return (
        <div>
            Main
        </div>
    );
}, compareFn);

export default Main;